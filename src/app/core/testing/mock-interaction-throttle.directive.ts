import { InteractionThrottleDirective } from '../../directives/component-throttle/interaction-throttle.directive';

export class MockInteractionThrottleDirective extends InteractionThrottleDirective {
  delay = 0;
}
